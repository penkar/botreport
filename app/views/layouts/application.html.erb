<!DOCTYPE html>
<html>
  <head>
    <title>Botreport</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Raleway:300' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Play' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Radley:400italic' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <nav class='navbar navbar-fixed-top navbar-dark bg-inverse'>
      <a class="navbar-brand" href="#">Bot Report</a>
      <div class="pull-xs-right">
      	<% if current_user %>
          <ul class="nav navbar-nav">
          	<li class="nav-item active"><%= link_to 'Home', root_path, class:'nav-link' %></li>
          	<li class="nav-item active"><%= link_to 'Your Page', user_path(current_user.id), class:'nav-link' %></li>
          	<li class="nav-item active"><%= link_to 'New Report', new_user_project_path(current_user.id), class:'nav-link' %></li>
          	<li class="nav-item active"><%= link_to 'News', user_projects_path(current_user.id), class:'nav-link' %></li>
          	<li class="nav-item active"><%= link_to 'Euro Opts', options_path(current_user.id), class:'nav-link' %></li>
          	<li class="nav-item active"><%= link_to 'Amer Opts', american_options_path(current_user.id), class:'nav-link' %></li>
          	<li class="nav-item active"><%= link_to 'GGM', ggm_path(current_user.id), class:'nav-link' %></li>
            <li class="nav-item active"><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete, class:'nav-link' %></li>
          </ul>
        <% else %>
          Welcome to the BotReport. You currently are not  <span style="color:red"><%= link_to 'signed in', new_user_session_path %></span>.
        <% end %>
      </div>
    </nav>

    <div class="header clearfix">
    	<div class="title">
    		<span class='tag'>One Step Closer to the Robot Apocalypse</span><br>
    	</div>
    </div>
    <div id='primary-content'>
    	<div class="content">
    		<%= yield %>
    	</div>
    </div>
    <script src="/assets/transitions.js" type="text/javascript"></script>
  </body>
</html>
